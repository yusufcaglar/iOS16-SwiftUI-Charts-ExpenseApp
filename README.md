# iOS16-SwiftUI-Charts-ExpenseApp

A simple Expense Tracker App, designed with SwiftUI.
You can import your data as json format to change the outcome.
I use a Singleton Budget struct, which is in Model.
